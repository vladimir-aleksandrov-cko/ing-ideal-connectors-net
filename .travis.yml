sudo: required

services:
  - docker

script:
  - chmod +x build.sh && docker run -it  -v /var/run/docker.sock:/var/run/docker.sock -v ${PWD}:/src danielmunchcko/dotnet-mono-aws:2.1.302-mono-5.12.0.301-aws-1.15.65 ./build.sh -t "Deploy"

# Travis CI clones repositories to a depth of 50 commits, which breaks the GitVersion..
git:
  depth: false

notifications:
  slack: checkout:CX121TjmlUo3RMtmNAaTHTCt