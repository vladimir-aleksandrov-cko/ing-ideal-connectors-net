# common configuration for ALL branches
image: ubuntu1804

test: off
cache:
- tools -> build.cake

build_script:
  - sh: chmod +x build.sh && ./build.sh -Target "Deploy"

artifacts:
  - path: 'artifacts\*.nupkg'